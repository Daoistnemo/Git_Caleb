# Some sample Serenity configuration options

#
# WebDriver configuration
#
webdriver {
  driver = chrome
  autodownload = true

  capabilities{
      "goog:chromeOptions" {
           args = [
                    "start-maximized", "test-type", "no-sandbox", "ignore-certificate-errors",
                    "disable-popup-blocking", "disable-default-apps", "disable-extensions-file-access-check",
                    "incognito", "disable-infobars", "disable-gpu", "remote-allow-origins=*"
                   ]
      }
  }
}

headless.mode = true

serenity {
  test.root = SwagLabs
  compress.filenames = false
  // take.screenshots = FOR_FAILURES

  # Custom Fields for Application and Environment
  custom.fields {
    version = 1.0.0
    environment = Desarrollo
  }

  # System Information (sysinfo) for test reports
  sysinfo {
    executedBy = ${user.name}
    java.version = ${java.version}
  }
}
